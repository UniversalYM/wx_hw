
<view class="video-list" wx:for="{{movieList}}" wx:key="user">
  <view class="video-title">{{item.title}}</view>
  <view class="video-time">时间：{{item.time}}</view>
  <txv-video vid="{{item.vid}}" playerid="{{item.vid}}"></txv-video>
</view>